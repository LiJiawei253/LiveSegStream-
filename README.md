# LiveSegStream-
 Real-time camera streaming with AI background removal. Captures from DirectShow, runs TensorRT portrait segmentation  on GPU, composites with blur, encodes via NVENC, and pushes to RTMP — all in a low-latency C++17 pipeline. And the whole pipeline run on the GPU, achieving zero copy.
